version: '3.8'

services:
  backend:
    build: ./backend
    container_name: coauths_backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/app:/app/app
    restart: always

  # Optional: Redis for caching if needed later
  # redis:
  #   image: redis:alpine
  #   ports:
  #     - "6379:6379"
